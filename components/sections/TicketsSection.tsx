import { eventContent } from "../../content/event"; import { ticketHref } from "../../content/eventConfig"; import { SectionHeading } from "../shared/SectionHeading"; export function TicketsSection(){return <section className="tickets tickets--experience" id="ingressos"><SectionHeading eyebrow="Escolha como entrar" title="A decisão começa antes do encontro." description="Escolha a experiência que corresponde ao seu momento." align="center"/><div className="ticket-grid">{eventContent.tickets.map(t=><article className={`ticket-card ${t.featured?"ticket-card--featured":""}`} key={t.name}>{t.featured?<span className="ticket-label">Experiência VIP</span>:null}<p className="ticket-name">{t.name}</p><p className="ticket-price">{t.price}</p><p className="ticket-description">{t.description}</p><ul>{t.includes.map(i=><li key={i}>✓ {i}</li>)}</ul><a className="ticket-action" href={ticketHref(t.checkoutUrl)}>{t.cta} ↗</a></article>)}</div></section>}
