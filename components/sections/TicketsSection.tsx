import { eventContent } from "../../content/event"; import { ticketHref } from "../../content/eventConfig"; import { SectionHeading } from "../shared/SectionHeading"; import { TicketCheckoutLink } from "../shared/TicketCheckoutLink"; const ticketTracking: Record<string, { id: string; value: number; quantity: number }> = { Individual: { id: "individual", value: 197, quantity: 1 }, Dupla: { id: "dupla", value: 347, quantity: 2 }, VIP: { id: "vip", value: 1197, quantity: 1 } }; export function TicketsSection(){return <section className="tickets tickets--experience" id="ingressos"><SectionHeading eyebrow="Escolha como entrar" title="A decisão começa antes do encontro." description="Escolha a experiência que corresponde ao seu momento." align="center"/><p className="lot-notice tickets-lot-notice">{eventContent.lotNotice}</p><div className="ticket-grid">{eventContent.tickets.map(t=>{ const tracking = ticketTracking[t.name]; return <article className={`ticket-card ${t.featured?"ticket-card--featured":""}`} key={t.name}>{t.featured?<span className="ticket-label">Experiência VIP</span>:null}<p className="ticket-name">{t.name}</p><p className="ticket-price">{t.price}</p><p className="ticket-description">{t.description}</p><ul>{t.includes.map(i=><li key={i}>✓ {i}</li>)}</ul><TicketCheckoutLink className="ticket-action" href={ticketHref(t.checkoutUrl)} ticketName={t.name} ticketId={tracking.id} value={tracking.value} quantity={tracking.quantity}>{t.cta} ↗</TicketCheckoutLink></article>})}</div></section>}
